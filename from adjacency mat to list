from collections import defaultdict


N = int(input())
lis = {}

for i in range(N):
    c = list(map(int,input().split()))
    now = [j + 1 for j in range(N) if c[j] == 1]
    print(len(now),*now)


    
